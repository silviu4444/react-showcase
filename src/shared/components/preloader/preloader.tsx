const Preloader = () => {
  return (
    <div className="fixed inset-0 z-[1000] flex h-screen w-full items-center justify-center bg-background">
      <div className="text-gradient cursor-default text-[32px] font-bold">
        immobilis
      </div>
    </div>
  );
};

export default Preloader;
